name: Check exercise order

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  call-gha-workflow:
    name: Check exercise order
    uses: IsaacG/github-actions/.github/workflows/sorted.yml@main
    with:
      ordering: difficulty-then-slug
